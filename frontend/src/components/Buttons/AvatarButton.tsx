import { IconButton } from "@chakra-ui/react";
import { QuestionOutlineIcon } from "@chakra-ui/icons";

const AvatarButton = () => {
  return (
    <IconButton aria-label="placeholder icon" icon={<QuestionOutlineIcon />} />
  );
};

export default AvatarButton;
